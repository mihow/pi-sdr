Continue scanner v2 implementation on the OpenWebRX+ fork (`~/Projects/Radio/OpenWebRX/openwebrx+` branch `feat/scanner-v2`, 102 tests passing). Read the status report at `~/Projects/Radio/OpenWebRX/openwebrx+/docs/scanner-v2-status.md` and dev improvements doc at `~/Projects/Radio/OpenWebRX/openwebrx+/docs/scanner-v2-dev-improvements.md` for full context. Priority 1: wire the recorder into the scan loop and populate the DB with recordings from IQ file scanning — the recorder (`owrx/scanner/recorder.py`), playback endpoint (`/api/scanner/recordings/{id}`), and UI play buttons all exist but aren't connected yet. Use `scripts/scan_iq.py` with `--demod-all` to generate WAV files, then verify playback in the mobile UI log view. Priority 2: test the ADPCM SYNC fix for live audio — last session found the scanner's audio decoder was missing SYNC word handling (now fixed in `htdocs/scanner/audio.js`), restart the `scanner-dev` Docker container and test at `http://localhost:8073/scanner`. Priority 3: implement offset tuning — retune SDR ±100 kHz from target signal and use `offset_freq` in DSP to avoid DC spike (research found OpenWebRX+ uses `lfo_offset` profile parameter). The DSP functions are now consolidated in `owrx/scanner/dsp.py`, scan loop has integration tests with mock callbacks in `test/scanner/test_scan_loop.py`. SDRplay RSP1a is on the dev machine — ensure only one `sdrplay_apiService` daemon runs (host service is masked, container manages its own).
